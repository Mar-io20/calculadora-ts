import React from "react";

const Button: React.FC = () => {
  return (
    <div className="btn numeros">
      <button type="submit"><input type="text" placeholder="Nome"></input></button>
    </div>
  );
};

export default Button;
